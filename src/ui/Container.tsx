import styled from '@emotion/styled';

interface Props {}
const Container = styled.div<Props>`
  margin: 24px auto;
  padding: 0 24px;
  max-width: 960px;
  width: 100%;
`;

export default Container;